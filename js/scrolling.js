$(function() {
    // Initialize Skrollr
    var s = skrollr.init({
        render: function(data) {
            //Uncomment the following line to turn on debugging logs
            //console.log(data.curTop);
        }
    });
});
